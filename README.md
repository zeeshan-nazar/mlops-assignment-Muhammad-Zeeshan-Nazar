# mlops-assignment-Muhammad-Zeeshan-Nazar
# MLOps Project - <STUDENT_ID>

## Project Description

This project demonstrates a basic MLOps workflow using Git, GitHub,
Visual Studio Code, and Python.

The project trains a Random Forest regression model for house price
prediction.

## Project Structure

```text
mlops-project-<STUDENT_ID>/
├── data/
├── src/
│   └── train_<STUDENT_ID>.py
├── model/
├── .gitignore
├── requirements.txt
└── README.md


The assignment asks the README to contain the commands needed to install dependencies and run the training script from the project root.
---

# Part 2 — `.gitignore`

This is one of the **most important parts** of the assignment.

## Step 13: Create `.gitignore`

Create:

```text
.gitignore